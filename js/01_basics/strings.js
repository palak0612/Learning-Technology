//string denoted both in '' "" and `` a string is an object that represents a sequence of characters.
let str1 = "this is a string"
let str2 = 'this is also a string'
let str3 = `this is also a string`

const name = "pal"
console.log(`hello my name is ${name} and my repo cnt is ${str1}`);  // $ is used in js to
//template literals use backticks ``
console.log(name.charAt(1));
console.log(name.indexOf('a'));
// .substring(), .slice()
// trim used to remove extra spaces from the code




